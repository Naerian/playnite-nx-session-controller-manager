<Window x:Class="ControllerSessionManager.PlayniteIntegration.ColorPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="" Width="468" SizeToContent="Height"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource PopupBackgroundBrush}"
        Foreground="{DynamicResource TextBrush}"
        TextElement.Foreground="{DynamicResource TextBrush}"
        SnapsToDevicePixels="True"
        PreviewKeyDown="OnPreviewKeyDown">
    <Window.Resources>
        <Style x:Key="PickerButton" TargetType="Button">
            <Setter Property="MinHeight" Value="36" />
            <Setter Property="Padding" Value="16,6" />
            <Setter Property="Margin" Value="8,0,0,0" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource GlyphBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              TextElement.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource HoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.86" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PickerLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Opacity" Value="0.78" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Margin" Value="0,0,0,5" />
        </Style>
        <DrawingBrush x:Key="CheckerBrush" TileMode="Tile" Viewport="0,0,10,10" ViewportUnits="Absolute">
            <DrawingBrush.Drawing>
                <DrawingGroup>
                    <GeometryDrawing Brush="#FFB8B8B8">
                        <GeometryDrawing.Geometry>
                            <RectangleGeometry Rect="0,0,10,10" />
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <GeometryDrawing Brush="#FF8A8A8A">
                        <GeometryDrawing.Geometry>
                            <GeometryGroup>
                                <RectangleGeometry Rect="0,0,5,5" />
                                <RectangleGeometry Rect="5,5,5,5" />
                            </GeometryGroup>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                </DrawingGroup>
            </DrawingBrush.Drawing>
        </DrawingBrush>
    </Window.Resources>
    <Border BorderBrush="{DynamicResource GlyphBrush}" BorderThickness="1" CornerRadius="4"
            Background="{DynamicResource PopupBackgroundBrush}">
        <DockPanel LastChildFill="True" Margin="18,16">
            <Border DockPanel.Dock="Top" BorderBrush="{DynamicResource GlyphBrush}"
                    BorderThickness="0,0,0,1" Padding="0,0,0,10" Margin="0,0,0,16">
                <TextBlock x:Name="TitleText" FontSize="20" FontWeight="SemiBold"
                           Foreground="{DynamicResource GlyphBrush}" />
            </Border>
            <StackPanel DockPanel.Dock="Bottom" Margin="0,16,0,0">
                <TextBlock x:Name="HexLabel" Style="{StaticResource PickerLabel}" />
                <TextBox x:Name="HexBox" Height="32" VerticalContentAlignment="Center"
                         TextChanged="HexBoxTextChanged" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button x:Name="CancelButton" Style="{StaticResource PickerButton}" Click="CancelClick" />
                    <Button x:Name="ApplyButton" Style="{StaticResource PickerButton}" Click="ApplyClick" />
                </StackPanel>
            </StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="14" />
                    <ColumnDefinition Width="22" />
                    <ColumnDefinition Width="18" />
                    <ColumnDefinition Width="132" />
                </Grid.ColumnDefinitions>
                <Border x:Name="SvSurface" Height="220" CornerRadius="4" ClipToBounds="True"
                        Cursor="Cross" MouseLeftButtonDown="SvSurfaceMouseLeftButtonDown"
                        MouseMove="SvSurfaceMouseMove" MouseLeftButtonUp="SvSurfaceMouseLeftButtonUp">
                    <Grid>
                        <Border x:Name="HueFill" />
                        <Border>
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#FFFFFFFF" Offset="0" />
                                    <GradientStop Color="#00FFFFFF" Offset="1" />
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        <Border>
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#00000000" Offset="0" />
                                    <GradientStop Color="#FF000000" Offset="1" />
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        <Canvas x:Name="SvCanvas" IsHitTestVisible="False">
                            <Ellipse x:Name="SvThumb" Width="14" Height="14" Stroke="White"
                                     StrokeThickness="2" Fill="Transparent">
                                <Ellipse.Effect>
                                    <DropShadowEffect BlurRadius="2" ShadowDepth="0" Opacity="0.7" />
                                </Ellipse.Effect>
                            </Ellipse>
                        </Canvas>
                    </Grid>
                </Border>
                <Border Grid.Column="2" x:Name="HueBar" Height="220" CornerRadius="4" Cursor="Hand"
                        MouseLeftButtonDown="HueBarMouseLeftButtonDown"
                        MouseMove="HueBarMouseMove" MouseLeftButtonUp="HueBarMouseLeftButtonUp">
                    <Grid>
                        <Border CornerRadius="4">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#FFFF0000" Offset="0" />
                                    <GradientStop Color="#FFFFFF00" Offset="0.17" />
                                    <GradientStop Color="#FF00FF00" Offset="0.33" />
                                    <GradientStop Color="#FF00FFFF" Offset="0.50" />
                                    <GradientStop Color="#FF0000FF" Offset="0.67" />
                                    <GradientStop Color="#FFFF00FF" Offset="0.83" />
                                    <GradientStop Color="#FFFF0000" Offset="1" />
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        <Canvas x:Name="HueCanvas" IsHitTestVisible="False">
                            <Border x:Name="HueThumb" Width="22" Height="8" Margin="-2,0,0,0"
                                    BorderBrush="White" BorderThickness="2" CornerRadius="2"
                                    Background="Transparent">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="2" ShadowDepth="0" Opacity="0.7" />
                                </Border.Effect>
                            </Border>
                        </Canvas>
                    </Grid>
                </Border>
                <StackPanel Grid.Column="4">
                    <TextBlock x:Name="PreviewLabel" Style="{StaticResource PickerLabel}" />
                    <Border Height="72" CornerRadius="4" ClipToBounds="True"
                            Background="{StaticResource CheckerBrush}"
                            BorderBrush="{DynamicResource GlyphBrush}" BorderThickness="1">
                        <Border x:Name="PreviewSwatch" />
                    </Border>
                    <TextBlock x:Name="OpacityLabel" Style="{StaticResource PickerLabel}" Margin="0,16,0,5" />
                    <DockPanel>
                        <TextBlock x:Name="OpacityValueText" DockPanel.Dock="Right"
                                   FontWeight="SemiBold" VerticalAlignment="Center" Margin="8,0,0,0" />
                        <Slider x:Name="OpacitySlider" Minimum="0" Maximum="100" TickFrequency="1"
                                IsSnapToTickEnabled="True" ValueChanged="OpacitySliderChanged" />
                    </DockPanel>
                </StackPanel>
            </Grid>
        </DockPanel>
    </Border>
</Window>
